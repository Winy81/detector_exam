<h1>address#new</h1>

<%= form_with scope: :place, url: places_path, local:true do |f| %>
  Your postcode: <%= f.text_field :postcode %>
  <%= f.hidden_field :user_id, :value => current_user.id %>

<%= f.submit %>

<% end %>